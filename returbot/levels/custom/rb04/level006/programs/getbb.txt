extern void object::GoGetThatDarnBox()
{
    point pos;
    pos.x = 24;
    pos.y = -2;
    goto(pos);
    pos.x = 122;
    pos.y = 41;
    goto(pos);
	object item = radar(BlackBox);
	turn(direction(item.position));
	goto(item.position);
	grab();
	item = radar(SpaceShip);
	goto(item.position);
	
}
